{% extends "GloryAdminBundle::layout.html.twig"%}
{% block title %}
    Dashboard - {{parent()}}
{% endblock %}
{% block content %}
    <div class="row">
        {% for panel in dashboard %}
            {% if panel.controller is defined %}
                {{ render(controller(panel.controller)) }}
            {% endif %}
        {% endfor %}
    </div>
{% endblock %}